# Next-Generation AI Compiler Survey and Design Guide

This survey explains the AI compiler field and tests a design hypothesis: **the compiler increasingly becomes an agentic optimization system**. It turns evidence into choices, tradeoffs, and experiments for building the next compiler.

Runtime performance comes first. Developer productivity and portability are secondary goals of similar importance. Search cost is measured from the start. Expected artifact reuse determines whether performance exploration can precede cost reduction or whether deployment latency and payback constrain search immediately.

Start with the [survey and design guide](docs/SURVEY.md), or read the [PDF](publish/out/next-gen-ai-compiler-survey.pdf). The guide covers trends, competing optimization methods, necessary compiler functions, gaps, design guidelines, and forecasts for 2027, 2029, 2031, 2036, and beyond. It treats passes, lowering, representations, and agent organization as design choices to test. Each forecast has a review date and an observable outcome; confidence in direction, timing, and architectural form is recorded separately.

The guide now distinguishes application optimization, compiler evolution, and **controller self-improvement**. The controller discussion separates kernel-specific prompt evidence from broader agent research and gives a concrete experiment for evaluating future-search benefit.

## Where things belong

| Location | Purpose |
|---|---|
| [Survey and design guide](docs/SURVEY.md) | The single narrative, with the established sections 0 through 9. |
| [Architecture overview](docs/architecture-overview.svg) and [controller development](docs/controller-development.svg) | Two figures with explicit development feedback and separate final evaluation; generated by `scripts/build_blueprint.py`. |
| [Reference guide](reference/README.md) | Source digests, publications, product evidence, and implementations. |
| [Status](STATUS.md) | Coverage, change history, and next research questions. |
| `publish/` | PDF generation and presentation sources. |
| `.cursor/skills/survey/` | Repository workflow, evidence standards, and human-readable writing rules. |

The [existing presentation](publish/out/next-gen-ai-compiler-sharing.pdf) is an earlier snapshot. It has not yet been aligned with the revised design guidance and forecast horizons.

## Updating the guide

Read the repository Survey skill before editing. Keep recommendations and source evidence distinct, explain abbreviations at first use, and make each design choice understandable without decoding section symbols or record identifiers. Prefer descriptive links and concrete validation experiments.

Add or revise source digests and their index, then update the narrative and affected reference maps. Check consistency, update status, and rebuild the PDF:

```bash
python3 scripts/build_blueprint.py
python3 scripts/validate_survey.py
python3 publish/build_pdf.py
```

Work on main. Commit coherent changes and publish only when authorized; an explicit instruction not to push overrides repository defaults. See the [maintenance notes](docs/SETUP_GITHUB.md).

## Scope

The guide includes compilers for AI workloads, AI methods for compiler construction and optimization, kernel generation, distributed execution, validation, and hardware enablement through executable workloads and compiler feedback. General coding agents and chip-design automation are relevant only when the connection to compilation is demonstrated.

Source summaries are for research and education. Original works retain their copyrights.
