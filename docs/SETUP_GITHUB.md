# Git remote notes

This working tree already tracks `origin` on `main`. Prefer ordinary git workflows:

```bash
git add -A
git commit -m "Survey update: <short why>"
git push origin main
```

Do not embed this survey’s own repository URL or name into digests, PDF covers, or prediction text — cite external primary sources only.
