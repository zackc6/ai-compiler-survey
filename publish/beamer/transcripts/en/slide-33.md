# Slide 33: Working stance until conflicts settle

Nine numbered stances until checkpoints falsify or confirm the prediction. On-slide: numbered list, compact. Spoken: read each; tie to claim IDs A6/A7/A8 and conflicts C1–C10.

**1. Hybrid: agents search; compilers lower (not replace).**
Control plane vs data plane split from slide 6. C6-B lean holds through Horizon B (~2029–31).

**2. Magellan ∥ MLGO — parallel bets (C1).**
Evolved shippable C++ and in-tree neural advisors both live until public llvm patches or EmitC-MLGO customer default settles the race. Do not prematurely crown either.

**3. Discount single-number speedups (C2).**
Vendor headlines vs median/p90 on pinned traces. One kernel at +15% is not default-path evidence at +2.5% p50.

**4. Constrained actions + strong oracles (C3).**
Free IR rewrite is fragile (mlirAgent below identity). Lean: hints, ACFs, pass lists — advisory metadata the classical applicator validates.

**5. Demote generic SCM AI (C7).**
Forge Copilot ≠ compiler-oracle review. T9 provenance and domain admit machinery matter; generic code completion does not settle checkpoints.

**6. Coverage→performance; no autonomous chip design (C9 / C10).**
TritorX→KernelEvolve ladder is bring-up playbook, not EDA replacement. Agents propose dialect/ISA feedback; humans own tape-out.

**7. ~6–7 data-plane bands; plugins if not consolidated (A6 / S6).**
L1–L6 must-have today; L7* fleet maturing. Agents unify contracts across bands, not one mega-IR. Power, $/token, SLOs become objectives + oracles, not new IR bands.

**8. E2E-optimal-*seeking*: joint search under F (A7 / S7).**
Product fitness F — latency, energy, $/token, quality, cluster — as Pareto/constrained front. Joint/bilevel search with F-admit and freeze; not claimed unique global optimum.

**9. Soft merge M1 by Horizon B; hard replace M3 not claimed (A8 / C6).**
M1: e2e controller + admit/trace over band tools; data plane stays. M2: contested search-surface merge ~2029–31. M3: no classical admit/fallback — not claimed. “I only talk to an agent” is often M1 + freeze.

Closing beat: operate on these nine until Tier A / ★ evidence moves. Do not average disagreements across orgs.
